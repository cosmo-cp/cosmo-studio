// Keep the Workflow route available in the main shell while the page is still intentionally blank.
export default function WorkflowPage() {
    return <div className="flex h-full w-full flex-col p-4" />;
}
